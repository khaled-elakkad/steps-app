import { styled } from '@mui/material';
import Box from '@mui/material/Box';

export const AllStepsContainer = styled(Box)(({ theme }) => ({
  display: 'flex',
  flexDirection: 'column',
  marginTop: 20,
  marginRight: 20,
}));
